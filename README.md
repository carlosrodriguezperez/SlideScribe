# SlideScribe-CLI

A lightweight, zero-UI CLI tool that converts lecture slide PDFs into Markdown notes using Vision LLMs to extract text and math formulas into LaTeX.

## Installation

```bash
pip install -e .
```

## Usage

```bash
slidescribe /path/to/lecture.pdf
```
